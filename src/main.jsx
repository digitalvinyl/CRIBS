import React from "react";
import ReactDOM from "react-dom/client";
import CribsApp from "./App";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <CribsApp />
  </React.StrictMode>
);
